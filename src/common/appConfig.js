const config = {

    _ENV: "debug", // "production";     // sample json will be used for environment !== production
    _IN_DATE_FORMAT:  'YYYY-MM-DD',     // input date format
    _OUT_DATE_FORMAT: 'DD/MM/YYYY',     // output date format
    _ITEM_COUNT_THRESHOLD: 1000,        // max number of items allowed before showing warning

}

export default config;
